# 검증 안내서 — 이미지+문구 카드 메이커

**관련 문서:** [[meme-card-maker/README|README]] · [[meme-card-maker/docs/00_과제_요구사항_매핑|과제 요구사항 매핑]] · [[meme-card-maker/AI_3줄|AI 3줄]]

## 어디로 가나요

```
https://meme-card-maker.vercel.app
```

> 소스 코드: https://github.com/stulss/meme-card-maker
>
> 로컬에서 확인하려면 상위 폴더에서 아래를 실행하고 `http://localhost:8127`로 접속하세요.
> ```
> python -u ".claude/serve.py" 8127 meme-card-maker
> ```

## 무엇을 하나요 (3단계, 30초 이내)

1. 위 공개 주소로 접속합니다. 편집 화면이 바로 보입니다(별도 로딩·로그인 없음).
2. 화면비를 **4:5** 또는 **9:16**으로 바꿔보고, "문구" 칸의 텍스트를 수정해봅니다.
   미리보기가 즉시 바뀝니다.
3. **PNG 다운로드** 버튼을 눌러 파일을 저장하고 열어봅니다. 미리보기와 똑같은
   배치로 저장됩니다.

## 무엇이 보이면 통과인가요

- **이미지·문구 편집 조작이 화면에 모두 보인다** — 이미지 업로드 버튼, 문구 입력칸,
  위치·크기·색 조절 슬라이더/피커가 편집 패널에 있습니다.
- **한 번 바꾼 값이 미리보기에 즉시 반영된다** — 문구를 고치거나 색을 바꾸면
  버튼을 따로 누르지 않아도 오른쪽 미리보기가 바로 바뀝니다.
- **세 비율 선택이 화면에서 구분된다** — `1:1` / `4:5` / `9:16` 버튼 중 선택된
  것이 파란색으로 강조되고, 미리보기 캔버스의 가로세로 비율도 함께 바뀝니다.
- **다운로드 파일이 미리보기와 일치한다** — PNG/JPG로 저장한 파일을 열어보면
  화면에서 본 것과 문구 위치·잘림·줄바꿈이 동일합니다.
- **극단적인 문구도 잘리거나 겹치지 않는다** — "검사 문구 빠른 입력" 드롭다운에서
  긴 한글, 이모지, 오버플로우 강제 단락 등을 선택해도 글자가 이미지 밖으로 튀어나오지
  않습니다(자동으로 작아지거나, 상자 안에서 잘립니다).
- **템플릿 생성·불러오기·수정·삭제가 화면에 보인다** — "템플릿" 패널에서 이름을
  입력하고 "새 템플릿으로 저장"을 누르면 목록에 추가되고, "불러오기"·"삭제" 버튼이
  각 항목에 있습니다. 새로고침해도 목록이 남아 있습니다.
- **가져오기·내보내기와 공개 정보 안내가 보인다** — "JSON으로 내보내기"·"JSON 가져오기"
  버튼과, 그 아래 "브라우저에만 저장되며 서버로 전송되지 않습니다"라는 안내 문구가
  항상 보입니다.
- **콘솔에 빨간 오류가 없다** — F12 → Console 탭에 오류가 0건입니다.

## 안 될 때

- **화면이 비어 있다** → 새로고침(F5)하세요. (구버전에서는 탭이 백그라운드로
  열렸을 때 첫 화면이 그려지지 않는 문제가 있었으나 수정 완료 — `트러블슈팅.md` 참고)
- **다운로드 파일이 안 열린다** → PNG/JPEG 형식인지, 파일 크기가 0바이트가 아닌지
  확인하세요.
- **새로고침 후 템플릿이 사라졌다** → 브라우저 개인정보 보호 모드이거나 사이트
  데이터가 차단되어 있을 수 있습니다. F12 → Application → Local Storage에서
  `meme-card-maker:templates:v1` 키가 있는지 확인하세요.
- **JSON 가져오기가 거부됐다** → 두 가지 경우가 있습니다: (1) JSON 문법 오류 —
  `tools/fixtures/import-invalid/broken-syntax.json` 참고 (2) 필수 항목 누락 —
  `tools/fixtures/import-invalid/missing-fields.json` 참고. 두 파일 모두 의도적으로
  깨뜨린 예시이며, 이런 파일을 넣어도 **기존에 저장해둔 템플릿은 사라지지 않습니다.**
- **폰트가 이상하게 보인다** → 네트워크 문제로 Pretendard 폰트가 로드되지 않으면
  시스템 기본 글꼴로 대체되어 보입니다. 기능에는 영향 없습니다.
