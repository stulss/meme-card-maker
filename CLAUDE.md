# meme-card-maker 작업 규칙 (Claude Code 세션 시작 시 자동 로드)

> 이 폴더에서 작업할 때 **폴더 전체를 read하지 말 것.** 아래 규칙을 따를 것.

## 1. 세션 시작 시 가장 먼저 할 일

폴더 목록을 훑어보는 대신, **`작업내역_체크리스트.md` 한 파일만** 먼저 읽는다.
그 파일 안의 `0. AI 필독 — 작업 프로토콜` 섹션이 이후 작업 순서(시작 전/중/종료 시)를 전부 정의한다.

## 2. 절대 통째로 읽지 말 것 (토큰 낭비)

| 경로 | 이유 |
|---|---|
| `img/` | 검증 스크린샷·완성 샘플 이미지 다수. 필요해도 개별 파일 1~2장만 |
| `assets/sample-images/`, `assets/fonts/` | 바이너리 에셋. 내용 확인은 필요할 때만 |
| `tools/fixtures/import-invalid/` | 의도적으로 깨뜨린 테스트용 JSON. 내용은 이미 문서화됨 |
| `js/` 전체 | 작업과 관련된 파일 1~2개만 골라 읽는다. 디렉토리 전체를 한 번에 읽지 않는다 |

## 3. 작업 유형별로 이 문서 하나만 읽기

| 하려는 작업 | 읽을 문서 |
|---|---|
| 설계·기술 결정 확인 | `docs/01_기획.md` |
| 배포 방법·절차 | `docs/05_배포.md` |
| 과제 요구사항 대조 | `docs/00_과제_요구사항_매핑.md` |
| 엣지 케이스 테스트 결과 | `docs/03_엣지케이스_테스트_결과.md` |
| 과거 문제 해결 사례 | `트러블슈팅.md` |
| 진행 상황·다음 할 일 | `작업내역_체크리스트.md` |

## 4. 코드 수정 시

- 어떤 파일을 고쳐야 하는지 알고 있다면 그 파일만 연다 (예: `js/render/TextLayout.js`).
- 모를 때만 `js/` 하위 디렉토리 목록을 보되, 목록만 보고 내용을 전부 읽지는 않는다.
- 기존 파일을 통째로 다시 쓰지 않는다 — 필요한 부분만 수정한다.

- **코드를 고쳤으면 그 작업은 아직 끝난 게 아니다.** 관련 문서를 갱신해야 작업이 종료된 것으로 친다.
  - `작업내역_체크리스트.md`의 작업 로그·진행사항 체크리스트에 반영
  - 동작이나 구조가 바뀌었으면 `README.md`, `docs/01_기획.md`, `docs/05_배포.md` 등도 같이 수정
  - "나중에 한꺼번에 정리" 하지 않는다 — 코드 변경과 문서 갱신을 같은 작업 단위로 묶는다

### 4-1. 커밋 전 문서 갱신 체크리스트

- [ ] 새 기능/동작을 추가했다 → README·기획 문서 갱신했는가
- [ ] 구조·파일 위치가 바뀌었다 → README의 폴더 구조 갱신했는가
- [ ] 배포 방식·설정이 바뀌었다 → `docs/05_배포.md` 갱신했는가
- [ ] 문제를 발견하고 해결했다 → `트러블슈팅.md`에 기록했는가
- [ ] 새로운 결정을 내렸다 → `작업내역_체크리스트.md`의 결정 기록에 추가했는가
- [ ] 어쨌든 뭔가 했다 → `작업내역_체크리스트.md`의 작업 로그에 남겼는가

## 5. 이 파일 자체 수정 규칙

새로 생긴 대형 폴더/파일이 있으면 2번 표에 한 줄 추가한다.
