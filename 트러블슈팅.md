# 트러블슈팅

**관련 문서:** [[meme-card-maker/README|README]] · [[meme-card-maker/작업내역_체크리스트|작업내역 체크리스트]] · [[meme-card-maker/docs/03_엣지케이스_테스트_결과|엣지케이스 테스트 결과]]

## 1. 최초 로드 시 미리보기 캔버스가 비어 보임 (requestAnimationFrame 지연)

**증상**: 앱을 처음 열면 편집 패널·템플릿 패널은 정상인데 미리보기 캔버스만 완전히
비어 있었다(투명). 콘솔 에러는 없었다.

**원인 조사**:
1. `CardRenderer.renderCard()`를 브라우저 콘솔에서 직접 호출해보니 즉시 정상적으로
   그려졌다 → 렌더링 로직 자체는 문제 없음.
2. `EditorController`의 초기 렌더가 `scheduleRender()` → `requestAnimationFrame()`
   경로에만 의존하고 있었다.
3. `document.hidden` / `document.visibilityState`를 확인하니 개발 중이던 브라우저
   탭이 "보이지 않는(hidden)" 상태로 판정되고 있었다 — 이 경우 브라우저는 `rAF`
   콜백 실행을 사실상 무기한 미룬다(실측: 4초 이상 대기해도 콜백이 호출되지 않음).

**결론**: 이는 이 프로젝트만의 버그가 아니라, "탭이 백그라운드/숨김 상태로 열렸을 때
`rAF`에만 의존해 첫 화면을 그리면 화면이 영원히 비어 보일 수 있다"는 일반적인 함정이다.
(예: 새 탭에서 배경으로 열기, iframe에 아직 보이지 않는 상태로 삽입 등.)

**수정**: `EditorController`에 `_renderNow()`를 추가해, 생성자에서 최초 1회는
`rAF`를 거치지 않고 `await CardRenderer.renderCard(...)`를 직접 호출하도록 변경.
이후 실시간 편집 중 재렌더(슬라이더 드래그 등)는 기존처럼 `scheduleRender()`의
`rAF` 배치를 그대로 사용한다 — 빠른 연속 입력에서 중복 렌더를 막는 이점은 유지하되,
첫 페인트만 즉시 실행되도록 분리했다.

```js
// Before: 생성자가 scheduleRender()만 호출 → 첫 페인트가 rAF에 종속됨
this.scheduleRender();

// After: 첫 페인트는 즉시, 이후 편집은 rAF 배치
this._renderNow();
```

**검증**: 수정 후 `document.hidden === true`인 상태에서도 캔버스에 배경색이
즉시 그려짐을 픽셀 단위로 확인(`getImageData` 값이 배경색 `#1c1c28`과 일치).

---

## 2. overflow-paragraph fixture가 실제로는 클립을 유발하지 못함

`docs/03_엣지케이스_테스트_결과.md`의 "결함 상세" 섹션에 전체 내용 기록.
요약: 최초 작성한 검사 문구(약 280자)가 최소 폰트 크기에서도 상자에 다 들어가버려
클립 안전망을 검증하지 못하는 상태였다. 브라우저에서 실측(`didClip` 값 확인)하며
문자 수를 늘려 4500자(60회 반복)로 교체, 클립 발동을 실제로 확인했다.

---

## 3. (사전에 피한 문제) Canvas tainting

이미지 업로드 시 `URL.createObjectURL()` + `<img>` + `canvas.drawImage()` 조합을
쓰면 브라우저에 따라 canvas가 "tainted" 상태가 되어 `toDataURL()`/`toBlob()` 호출 시
`SecurityError`가 발생할 수 있다. 이 프로젝트는 처음부터 `FileReader.readAsDataURL()`로
파일을 완전히 메모리 내 base64 문자열로 바꿔서 다루므로, canvas가 tainted될 경로 자체가
없다. 실제로 문제를 겪은 뒤 고친 게 아니라, 설계 단계에서 미리 피한 결정이다.
(`js/utils/fileHelpers.js` 상단 주석 참고)

---

## 4. (사전에 피한 문제) localStorage quota 초과

업로드 이미지를 원본 그대로 dataURL로 저장하면 몇 MB짜리 사진 하나만으로도
localStorage quota(보통 5~10MB)를 넘길 수 있다. 이 문제를 실제로 겪기 전에,
템플릿 저장 전 이미지를 최대 1280px로 downscale + JPEG/PNG 압축하도록 설계했다
(`fileHelpers.toDataUrlDownscaled`). `TemplateStore.saveAll()`도 `QuotaExceededError`를
잡아서 사용자에게 친절한 메시지로 안내하도록 방어 코드를 넣었다.
