# 이미지+문구 카드 메이커 — 작업내역 체크리스트 (AI 공용 인수인계 파일)

**관련 문서:** [[meme-card-maker/README|README]] · [[meme-card-maker/docs/00_과제_요구사항_매핑|과제 요구사항 매핑]] · [[meme-card-maker/docs/01_기획|기획]] · [[meme-card-maker/docs/05_배포|배포 절차]] · [[meme-card-maker/검증안내서|검증안내서]] · [[meme-card-maker/AI_3줄|AI 3줄]]

> **이 파일은 여러 AI 모델이 이어달리기로 하나의 프로젝트를 완성하기 위한**
> **유일한 단일 진실 공급원(Single Source of Truth)입니다.**

---

## 0. AI 필독 — 작업 프로토콜 (반드시 이 순서대로)

### ▶ 작업 시작 전 (BEFORE)
1. **이 파일 전체를 먼저 읽는다.**
2. `## 6. 다음 작업자에게`를 읽고 이어받을 지점을 파악한다.
3. `## 3. 진행사항 체크리스트`에서 가장 위의 미완료 항목 1개를 고른다.
4. `## 5. 결정 기록`을 읽고 이미 확정된 결정을 뒤집지 않는다.

### ▶ 작업 중 (DURING)
- 기존 파일을 통째로 다시 쓰지 않는다. 필요한 부분만 수정한다.
- 새 결정을 내렸다면 즉시 `## 5. 결정 기록`에 추가한다.

### ▶ 작업 종료 시 (AFTER)
1. `## 2. 현재 상태 스냅샷` 갱신
2. `## 3. 진행사항 체크리스트`에서 완료 항목 `[x]` 처리
3. `## 4. 작업 로그`에 실제로 만든/고친 파일 경로와 결과 기록
4. `## 6. 다음 작업자에게`를 완전히 새로 덮어쓴다

---

## 1. 프로젝트 개요 (30초 브리핑)

| 항목 | 내용 |
|---|---|
| 이름 | 이미지+문구 카드 메이커 (meme-card-maker) |
| 종류 | 이미지+문구를 조합해 밈·카드뉴스·SNS 이미지를 만드는 웹 도구 |
| 기술 스택 | 순수 HTML/CSS/JS (빌드 도구 없음), Canvas 2D API, localStorage |
| 배포 | GitHub 저장소 → Vercel 연동 (push마다 자동 배포) |
| 과제 범위 | 과제 카드 5개 (편집·미리보기 / 화면·파일 일치 / 극단 입력 / 템플릿 관리 / 가져오기·내보내기) |

## 2. 현재 상태 스냅샷

- [x] M0 Scaffold — 폴더 구조, 문서 뼈대
- [x] M1 폰트 — Pretendard Variable(OFL) 다운로드 완료
- [x] M2 Fixture 생성 — 5개 샘플 이미지 생성·커밋 완료
- [x] M3 핵심 렌더 — CardRenderer/ImageFit 완성, 단일 canvas 구조로 화면·파일 일치 보장
- [x] M4 텍스트 autofit — TextLayout 완성 (grapheme-safe wrap + 이진탐색 자동축소 + 클립 안전망)
- [x] M5 템플릿 CRUD — TemplateStore + UI 완성, localStorage 저장/복원 검증됨
- [x] M6 Import/Export — 유효성 검사 + 2개 invalid fixture 완성, round-trip 검증됨
- [x] M7 Edge case 테스트 문서화 — 12개 조합 결과 표 작성 완료
- [x] M8 최종 샘플 3개 이미지 생성 완료 (img/samples/)
- [x] M9 문서화 마무리 (README, CLAUDE.md, docs/00·01·03, 트러블슈팅, AI_3줄, 포트폴리오 소개글)
- [x] M10 보안 검토 완료 — 시크릿/자격증명/이메일/전화번호/주민번호 패턴 0건
- [x] M11 GitHub→Vercel 배포 완료 — GitHub repo(`stulss/meme-card-maker`) 생성/push, Vercel 연결,
      최초 배포는 자동화 API가 403으로 막혀 사용자가 Vercel 대시보드에서 직접 트리거함.
      공개 URL: https://meme-card-maker.vercel.app
- [x] M12 최종 검증 완료 — 공개 URL에서 콘솔 오류 0건, 문구/비율 즉시 반영, PNG export 정상,
      공개정보 안내 문구 노출 모두 실제 브라우저로 재확인함

## 3. 진행사항 체크리스트

- [x] index.html, css/style.css 작성
- [x] js/utils/{idGenerator,fileHelpers,validators}.js
- [x] js/models/{CardStateModel,TemplateModel,TemplateStore,FixtureData}.js
- [x] js/render/{ImageFit,TextLayout,CardRenderer}.js
- [x] js/views/{domHelpers,EditorView,TemplateListView,ImportExportView}.js
- [x] js/controllers/{EditorController,TemplateController,ImportExportController,AppController}.js
- [x] js/main.js
- [x] 브라우저 실행 검증: 12개 문구 × 3비율 = 36개 조합 렌더 오류 0건
- [x] 5개 fixture 이미지 × 2가지 fit mode 렌더 오류 0건
- [x] 템플릿 생성/수정/삭제 + 새로고침 후 유지 검증
- [x] JSON export/import round-trip + 손상 파일 2종 거부 검증
- [x] localStorage 손상값 자동 백업·복구 검증
- [x] docs/03_엣지케이스_테스트_결과.md에 결과표+결함 before/after 기록
- [x] 완성 샘플 이미지 3개 저장 (img/samples/)
- [x] 나머지 문서(README, CLAUDE.md, 트러블슈팅, AI_3줄, 포트폴리오 소개글 등) 작성
- [x] GitHub 저장소 생성 및 push (`https://github.com/stulss/meme-card-maker`)
- [x] Vercel 연동 및 배포 확인 (`https://meme-card-maker.vercel.app`, 콘솔 오류 0건)

## 4. 작업 로그

- 2026-08-21: 프로젝트 scaffold 생성. Pretendard Variable 폰트(OFL) 다운로드.
  Python(Pillow)로 5개 fixture 이미지 절차적 생성(square.png, portrait.jpg,
  landscape.jpg, transparent.png, photo-like.jpg) — 저작권/EXIF 문제 없음(직접 그림).
- 2026-08-21: 전체 JS 모듈(utils/models/render/views/controllers) + index.html/style.css
  작성 완료. 브라우저에서 실행 검증:
  - 최초 로드 시 캔버스가 비어 보이는 버그 발견 → 원인: 최초 페인트가 requestAnimationFrame에만
    의존했는데, 숨겨진/배경 탭에서는 브라우저가 rAF 호출을 미룬다 → EditorController에
    `_renderNow()`를 추가해 최초 페인트는 rAF 없이 즉시 실행하도록 수정. (트러블슈팅.md에 기록 예정)
  - 12개 텍스트 케이스 × 3비율 = 36개 조합 렌더 테스트 → 오류 0건
  - overflow-paragraph fixture가 실제로는 클립을 유발하지 못함을 발견(글자 수 부족) →
    브라우저에서 실측(canvas.measureText 기반)해 60회 반복(4500자)으로 교체, 클립 발생 확인
  - 템플릿 CRUD, JSON import/export, localStorage 손상 복구 모두 직접 실행 검증 완료
- 2026-08-22: 남은 문서(docs/00·01·03·05, 트러블슈팅, AI_3줄, 포트폴리오 소개글, 검증안내서)
  전부 작성. img/parity·img/edge-cases·img/samples에 실제 canvas export PNG 증거 파일 저장
  (before/after 결함 증거 포함, 빨간 테두리로 텍스트 상자 표시). 보안 검토(grep) 통과 —
  시크릿/자격증명/이메일/전화번호 0건. `git init`+커밋 → `gh repo create`로 GitHub 저장소
  생성/push (GitHub Desktop이 Git Credential Manager에 저장해둔 토큰을 재사용해 `gh` 인증,
  별도 로그인 불필요) → Vercel 프로젝트 연결. 자동화 API로 최초 배포 트리거 시 403 Forbidden
  (계정 권한 문제) → 사용자가 Vercel 대시보드에서 직접 배포 완료. 공개 URL
  `https://meme-card-maker.vercel.app`에서 콘솔 오류 0건, 문구/비율 즉시 반영, PNG export,
  공개정보 안내 문구 모두 재확인. 프로젝트 완료.
- 2026-08-22: 사용자가 "날짜 지정 → 그날 KBO 경기 결과로 카드뉴스 자동생성" 기능을
  요청. 조사 결과 무료 KBO 데이터 API 없음(공식 사이트 미제공, 상용은 유료) +
  CORS로 브라우저에서 직접 크롤링 불가 → 서버 추가 필요 + KBO 약관 리스크 있음을
  확인해 보고. 사용자가 야구 특화 아이디어를 취소하고, 대신 범용 "이미지·문구
  자동 생성" 기능(직접 입력도 항상 가능)으로 요청 변경.
  `js/render/AutoImageGenerator.js`(팔레트×도형 무작위 조합 배경 생성),
  `js/models/PhraseTemplates.js`(카테고리별 큐레이션 문구) 추가, `EditorView`·
  `EditorController`에 버튼 2개 연결. 브라우저에서 20회 무작위 조합 + 기존
  12개 텍스트 케이스 재검증 → 실패 0건, 회귀 없음 확인.
- 2026-08-22: 사용자가 "크롤링해도 괜찮다"고 해서 야구 기능 재추진. KBO 사이트
  네트워크 요청을 분석해 내부 웹서비스 엔드포인트와 정확한 요청 형식(form-urlencoded)을
  파악(브라우저에서 jQuery ajaxSend 후킹으로 실제 요청 본문 캡처). 월 130경기 수신 확인.
  `api/kbo-schedule.js`(서버리스 프록시+CDN 캐싱), `js/models/KboTeams.js`(10구단 색상),
  `js/render/BaseballCardGenerator.js`(배경·문구 생성), `js/controllers/BaseballController.js`
  (날짜→목록→카드) 추가. 배포 후 공개 URL에서 검증:
  - 정상 경기(한화 6:9 두산 → 두산 네이비 배경), 완승(롯데 11:0 SSG → '완승!' 자동 판정),
    무승부(LG 2:2 두산 → '무승부'), 취소(NC vs 두산 → '폭염취소'), 경기 없는 날(안내 문구)
    모두 정상 처리 확인
  - 기존 12개 텍스트 케이스 × 3비율 + 야구 배경 3종 × 3비율 + 템플릿 CRUD 회귀 테스트
    → 실패 0건, 콘솔 오류 0건

## 5. 결정 기록

- **단일 canvas 구조**: 미리보기와 export가 동일한 canvas를 쓴다 (화면=파일 구조적 보장).
- **텍스트 레이어는 1개만**: 과제 카드 요구사항이 "문구"(단수)이므로 v1은 텍스트 레이어 1개로 한정.
  `TemplateRecord.text`가 객체(배열 아님)인 이유.
- **이미지는 downscale 후 저장**: localStorage quota(5~10MB) 보호를 위해 업로드 이미지는
  최대 1280px로 축소 후 dataURL로 저장.
- **Import는 항상 append, 절대 replace 아님**: 손상된 가져오기 파일이 기존 템플릿을
  파괴할 수 없다는 보장의 핵심 설계.
- **최초 렌더는 rAF에 의존하지 않음**: 숨겨진/배경 탭에서 rAF가 지연되는 문제를 피하기 위해
  EditorController 생성 시 `_renderNow()`를 동기적으로(await) 한 번 호출한다.
- **날짜별 KBO 경기 결과 자동 연동 채택 (2026-08-22 결정 변경)**: 처음에는 무료 API
  부재로 기각했으나, 사용자가 크롤링을 허용해 재검토. KBO 사이트 내부 웹서비스
  엔드포인트(`/ws/Schedule.asmx/GetScheduleList`)가 JSON을 반환하는 것을 확인해
  HTML 크롤링보다 안정적인 방식으로 구현. CORS 회피를 위해 Vercel 서버리스 함수
  1개(`api/kbo-schedule.js`) 추가.
- **cron 대신 CDN 캐시 사용**: 과거 날짜 1일, 오늘/미래 10분 캐싱. 조회된 달만
  가져오므로 매일 전체를 긁는 cron보다 KBO 서버 부하가 적고 DB·스케줄러가 불필요.
- **구단 로고·엠블럼 이미지 미사용**: 저작권 문제를 피하기 위해 팀 상징색만 활용한다.
- **PRD 문서(card_meme_web_tool_development_request.md)의 스택(React+Konva)은 채택하지 않음**:
  과제 카드 5개 통과가 목표이므로 순수 HTML/JS 유지. 상세 근거는 `.claude/plans/key-velvet-kahan.md`
  "참고 문서 검토 및 범위 조정" 섹션 참고.

## 6. 다음 작업자에게

M0~M12 전체 완료. 공개 URL(`https://meme-card-maker.vercel.app`)에서 검증안내서 3단계를
직접 실행해 통과 확인함. 프로젝트는 제출 가능한 상태다.

추가로 손댈 여지가 있다면(선택 사항, 과제 범위 밖):
- Web Share API로 결과물 바로 공유 (PRD 2차 개발 항목, 미채택)
- 실행 취소/다시 실행 (PRD 2차 개발 항목, 미채택 — `## 8. 하지 말 것` 참고)
- Vercel 자동 배포 API 403 문제의 근본 원인(계정/팀 권한) 규명 — 지금은 대시보드
  수동 배포로 우회했고 GitHub 연동 자체는 정상이라 이후 push는 자동 재배포됨

## 7. 미해결 질문

(현재 없음)

## 8. 하지 말 것

- 텍스트 레이어를 여러 개로 확장하지 않는다 (과제 범위 초과, PRD 스택 전환과 마찬가지로 범위 밖).
- React/Konva 등으로 스택을 바꾸지 않는다.
- localStorage 대신 서버 저장으로 바꾸지 않는다 (개인정보 보호 원칙과 배포 단순성).
