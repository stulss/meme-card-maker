# 포트폴리오 추가용 소개글

## 이미지+문구 카드 메이커

이미지와 문구를 조합해 밈·카드뉴스·SNS 이미지를 만드는 브라우저 기반 편집 도구.
설치나 로그인 없이 즉시 사용할 수 있으며, 프레임워크나 빌드 도구 없이 순수
HTML/CSS/JavaScript와 Canvas 2D API만으로 구현했다.

**핵심 도전 과제와 해결**
- *미리보기와 다운로드 파일이 항상 같아야 한다*: 별도의 export용 캔버스를 두지 않고,
  화면에 보이는 캔버스와 파일로 내보내는 캔버스를 물리적으로 하나로 통일해 "화면=파일"을
  구조적으로 보장했다.
- *어떤 입력에도 텍스트가 깨지지 않아야 한다*: 이진 탐색 기반 자동 폰트 축소와
  `Intl.Segmenter`를 이용한 grapheme 단위 줄바꿈(이모지 합성 시퀀스 보존)을 구현하고,
  최소 크기에서도 넘치는 경우를 대비한 클립 안전망을 뒀다. 긴 한글, 한영 혼합, 이모지,
  빈 문자열 등 12가지 극단 입력으로 검증했다.
- *서버 없이 사용자 데이터를 안전하게 관리해야 한다*: 템플릿은 브라우저 localStorage에만
  저장되며, 업로드 이미지는 저장 전 자동으로 축소·압축해 용량 초과를 방지한다. 손상된
  저장값이나 잘못된 JSON 가져오기 파일이 들어와도 기존 데이터는 항상 보존되도록
  방어적으로 설계했다.

**배운 점**: `requestAnimationFrame`은 탭이 숨겨진 상태에서 호출이 지연될 수 있다는
점을 실제로 겪으며, 최초 렌더링처럼 반드시 즉시 실행돼야 하는 부분과 편집 중 성능
최적화를 위해 배치해도 되는 부분을 구분해서 설계해야 한다는 것을 배웠다.
